import { DoomPortalPushDirective } from './doom-portal-push.directive';

describe('DoomPortalPushDirective', () => {
  it('should create an instance', () => {
    const directive = new DoomPortalPushDirective();
    expect(directive).toBeTruthy();
  });
});
